// All asset meshes and models are now defined as C++17 inline variables in
// Assets.h, so there is nothing to define here. This translation unit is kept
// only so the project file's reference to Assets.cpp stays valid.
#include "Assets.h"
